<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MessageDAO">
	<select id="selectMsgYetReadCount" resultType="int">
		select count(*) from message where recipient = #{id} and readOk = 'N'
	</select>
	<insert id="insertMsg">
		insert into message values(message_seq.nextval,#{sender},#{recipient},#{contents},default,default)
	</insert>
	<select id="selectAllMsgByCurrentPage" resultType="kh.spring.dto.MessageDTO">
		select * from (select row_number() over(order by MESSAGE_DATE desc) as rown, message.* 
		from message where 
		<if test="recipient!=null">
			recipient=#{recipient})
		</if>
		<if test="sender!=null">
			sender=#{sender})
		</if> 
		where rown between #{start} and #{end}
	</select>
	<update id="updateReadOkToYes">
		update message set readOk = 'Y' where seq = #{seq}
	</update>
	<select id="getMsgAllCount" resultType="int">
		select count(*) from message where 
		<if test="recipient!=null">
			recipient=#{recipient}
		</if>
		<if test="sender!=null">
			sender=#{sender}
		</if>
	</select>
</mapper>